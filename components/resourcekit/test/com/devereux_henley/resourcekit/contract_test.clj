(ns com.devereux-henley.resourcekit.contract-test
  (:require [clojure.test :as test :refer :all]
            [com.devereux-henley.resourcekit.contract :as resourcekit]))

(deftest dummy-test
  (is (= 1 1)))
